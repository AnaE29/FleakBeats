<ul class="py-2 text-sm text-gray-700 dark:text-gray-200">
    <% favorites.each do |favorite| %>
      <li class="block px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 dark:hover:text-white"><%= "#{favorite.track.playlist.name} - #{favorite.track.name.gsub("_", " ").titleize}" %></li>
    <% end %>
    </ul>